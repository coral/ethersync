[package]
name = "ethersync-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
repository = "https://github.com/coral/ethersync"
publish = ["crates-io"]
description = "Protocol and timing core for Ethersync LAN timecode synchronization"
build = "build.rs"

[dependencies]
prost.workspace = true
thiserror.workspace = true

[build-dependencies]
protox = "0.9.1"
prost-build = "0.14.4"
prost.workspace = true
